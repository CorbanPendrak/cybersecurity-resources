#command #terminal 
[[Linux Commands MOC]]
- - -

The `rm` command removes files and directories.

# Usage

`rm <file to remove>`

```shell
$ ls
flag.txt afolder
$ rm flag.txt
$ ls
afolder
```

## Important Flags

- `-r`: recursive, removes folder